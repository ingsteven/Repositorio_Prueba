import { Marcador } from './marcador';

describe('Marcador', () => {
  it('should create an instance', () => {
    expect(new Marcador()).toBeTruthy();
  });
});
describe('Marcador', () => {
  it('should create an instance', () => {
    expect(new Marcador()).toBeTruthy();
  });
});
